#include "main.h"

/**
 * main - prints a string, followed by a new line.
 *
 * @s: string to print
 *
 * Return: 0
 */

void _puts_recursion(char *s);
{
	char s[] = "Puts with recursion";
	{
		putchar(s);
	}
	putchar('\n');
return (0);
}

